Amargon's Void Memory OS 2.0
Enterprise Reliability & Cognitive Integrity Upgrade Plan
PHASE 0. Memory Integrity Baseline
Создание контрольной точки перед изменениями
Цель

Получить измеримый baseline текущего состояния.

Добавить:
0.1 Memory Health Snapshot

Создать:

memory/diagnostics/integrity_snapshot.py

Сбор:

количество фактов
количество активных/архивных
количество orphan embeddings
количество FAISS mappings
количество identity entries
количество broken relations
количество pending events
размер SQLite
размер FAISS

Пример:

Memory Integrity Score: 97.3%

Facts:
active: 73
dormant: 12
superseded: 40

Embeddings:
valid: 73
missing: 3

Identity:
categories: 6
consistency: OK
PHASE 1. IdentityVault Transaction Isolation
Атомная личность

(частично уже найдено)

1.1 Убрать прямые SQLite подключения

Сейчас:

IdentityVault
    |
    sqlite3.connect()

Проблема:

обход:

RLock
transaction wrapper
MemoryStore synchronization

Переделать:

IdentityVault
      |
MemoryDatabase
      |
transaction()
      |
SQLite
1.2 Создать Identity Unit Of Work

Новый механизм:

with identity_transaction():

    update(core_identity)

    update(values)

    update(fears)

    update(roles)

COMMIT

Всё или ничего.

1.3 Batch update_identity()

Вместо:

update_identity()
update_identity()
update_identity()
update_identity()

создать:

update_identity_batch({
core_identity:"",
values:"",
roles:""
})

Одна транзакция.

PHASE 2. ShadowEvaluator Safety Rewrite
Закрытие критической дыры F7

Приоритет: P0

2.1 Fail-open → Fail-closed

Сейчас:

except:
    return True

Нужно:

except:
    return False

Ошибка проверки = запрет записи.

Логика:

LLM unavailable
        |
        v
cannot verify
        |
        v
DO NOT MODIFY IDENTITY
2.2 Ввести Confidence Gate

Перед записью:

identity_update
       |
       v
ShadowEvaluator
       |
confidence?
       |
+------+------+
|             |
>=0.8        <0.8
|             |
WRITE       REVIEW
2.3 Audit trail

Каждый drift:

таблица:

identity_change_requests

Поля:

id
category
old_value
new_value
confidence
approved
source
created_at

Теперь история личности становится воспроизводимой.

PHASE 3. Reflection Pipeline Integrity
Восстановление обратной связи Reasoning → Memory

Исправление F6.

3.1 ReflectionBuffer становится обязательным шлюзом

Сейчас:

message
 |
importance
 |
UserModel

Нужно:

message

 ↓

ReasoningEngine

 ↓

ReflectionBuffer

 ↓

Governor

 ↓

UserModel

 ↓

IdentityVault
3.2 Запрет прямого reflection вызова

Удалить:

if importance >=7:
    create_task(reflection)

Заменить:

if reflection_decision.allowed:
       create_task()
3.3 Memory Write Policy

Создать:

MemoryWritePolicy

Решает:

можно ли писать
куда писать
какой confidence
нужен ли human review
PHASE 4. UserModel Concurrency Hardening
4.1 Исправить TOCTOU

F3.

Сейчас:

read state

unlock

await LLM

write state

Проблема:

между ними мир изменился.

Нужно:

создать version:

identity_version

Пример:

read version 15


LLM


before write:

version still 15?

YES:
 commit

NO:
 retry
PHASE 5. Database Migration Framework

Теперь можно безопасно менять схему.

Создать:

memory/migrations/

Структура:

001_identity_transactions.py

002_fact_embeddings.py

003_event_state.py

Перед миграцией:

backup sqlite

apply migration

validate

commit version

Добавить:

schema_version
PHASE 6. Embedding Ownership Rewrite
Отвязка векторов от фактов

Исправляет Kimi F1.

Сейчас:

facts
 |
embedding

Проблема:

удаление факта ломает вектор.

Новая схема:

facts

id
text


fact_embeddings

id
fact_id
model
dimension
vector
created_at

Плюсы:

смена моделей
несколько индексов
recovery
PHASE 7. Multi-Space FAISS Architecture
Новый индексный слой

Вместо:

faiss_index.bin

будет:

indexes/

gemini768/

faiss.bin

metadata.json


minilm384/

faiss.bin

metadata.json

metadata:

{
model:"gemini",
dimension:768,
count:1200,
created:"..."
}
PHASE 8. Atomic Fact Deduplication

Исправление Kimi F2.

Проблема:

check duplicate

insert


гонка.

Новое:

transaction:

LOCK

check

insert

commit


Добавить SQLite:

UNIQUE INDEX

на:

fact_hash
PHASE 9. Embedding Recovery System
Самовосстановление

Создать:

EmbeddingRecoveryWorker

Проверяет:

missing embeddings
wrong dimension
orphan vectors

Ограничения:

для слабого железа:

batch=10

sleep=0.5

max_cpu=20%
PHASE 10. EventBus Memory Consistency
Надежные события

Создать:

memory_events

State machine:

CREATED

↓

EMBEDDING_PENDING

↓

EMBEDDED

↓

INDEXED

↓

AVAILABLE

Если ошибка:

FAILED
PHASE 11. Dead Letter Queue
Ничего не теряем

Таблица:

dead_letter_events

Поля:

event_id

error

retry_count

last_attempt


После 3 ошибок:

DLQ

Команды:

/dlq_stats

/retry_dlq
PHASE 12. FAISS Crash Safety
Защита индекса

Никогда:

write faiss_index.bin

Только:

faiss_index.tmp

validate

rename()
PHASE 13. Retrieval Intelligence Upgrade
Исправление F1 bot_core

Сейчас:

substring matching.

Заменить:

embedding similarity

+
semantic entailment
+
LLM judge

Метрика:

context_utilization_score
PHASE 14. Resource Governance
Защита слабого железа

Все фоновые процессы через:

ResourceGovernor

Контролирует:

CPU
RAM
SQLite locks
API quota
PHASE 15. Actor Model User Isolation
Защита от гонок сообщений

Каждый пользователь:

UserActor(uid)

очередь:

message1

message2

message3

Один пользователь = последовательная обработка.

PHASE 16. Persistence Domain Separation

Не микросервисы.

DDD домены:

memory/

 identity/

 facts/

 embeddings/

 events/

 cognition/

 analytics/
PHASE 17. Memory Watchdog
Самодиагностика

Фоновый процесс:

раз в неделю:

проверяет:

orphan facts
orphan embeddings
broken relations
bad identity states
FAISS mismatch

И чинит.

PHASE 18. QA Engineering Layer

Создать полноценный QA слой.

Boundary tests:

Например:

confidence 0.49

confidence 0.50

confidence 0.51
Equivalence classes:
valid identity update

invalid identity update

contradictory update
Property tests:

Инварианты:

"fact без embedding не должен быть searchable"

"identity не может измениться без audit entry"

PHASE 19. Shutdown & Recovery System

Исправление F9.

Graceful shutdown:

stop accepting messages

finish active transactions

flush events

save FAISS

close database

exit
PHASE 20. Long-Term Memory Maintenance

Финальный слой.

Dreaming:

consolidation
merge duplicates
decay
importance recalculation
graph optimization
Итоговая архитектура после всех фаз:
                 User
                  |
             User Actor
                  |
            Cognitive Loop
                  |
          Reasoning Engine
                  |
        Reflection Governor
                  |
        Memory Write Policy
                  |
        Memory Domain Layer
                  |
    +-------------+-------------+
    |                           |
 IdentityVault             MemoryStore
    |                           |
 SQLite Transaction Layer
    |
 Events + Audit + Recovery

                  |
            Embedding Domain

                  |
             FAISS Manager

                  |
          Watchdog Self Healing

Это уже не просто "бот с памятью". После этих фаз это будет локальная когнитивная система с:

транзакционной памятью
самовосстановлением
аудитом изменений
защитой от галлюцинаций
версионированием личности
сменой embedding моделей без миграционного ада

И самое главное: порядок теперь правильный. Сначала закрываем коррупцию личности, потом фундамент БД, потом перестраиваем память. Иначе можно идеально сделать FAISS, пока UserModel тихо превращает личность в кашу 😄